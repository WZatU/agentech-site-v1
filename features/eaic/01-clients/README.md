# Client entry points

`eaic-hub/` owns the current web client implementation. Future CLI, macOS, and iOS clients can be added as sibling folders while sharing contracts through the unified API layer.
