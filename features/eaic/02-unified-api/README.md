# Unified API

This layer groups stable contracts consumed by EAIC clients. Current modules cover project validation and resource/run definitions; authentication and live-result route handlers remain under `app/api/` where Next.js requires them.
